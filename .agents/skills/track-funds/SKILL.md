---
name: track-funds
description: "대한민국 지방자치단체 기금(Funds)의 예산 규모·근거 조례·담당 부서·집행 사업·지역 연관성을 추적·관리한다. 메모리 인덱스 파일을 먼저 읽어 갱신 대상만 타겟 조사해 중복 검색을 방지한다. Use when: 지자체 기금 운용 현황 조사, 기금 마크다운 갱신, 생활권/행정구역별 영향 분석, 연도별 조성액·수입·지출 추적."
---

# track-funds

대한민국 지방자치단체 기금(Funds) 운용 현황을 **메모리 기반 조사 방식(Memory-driven Research)**으로 누적 관리한다.
중복 검색을 막고 갱신이 필요한 항목만 타겟 조사한다.

## 대상 지자체 식별

- 작업 시작 시 저장소 루트의 `location.txt`를 먼저 읽어 **대상 행정구역**을 확정한다.
- 본 스킬에서 `<지자체>`는 대상 지방자치단체, `<생활권>`은 해당 지자체 내부 지역 구분을 뜻한다.
- 사용자가 채팅에서 다른 지자체를 명시하면 그 지시가 `location.txt`보다 우선한다.

## 워크플로우

### 1. 메모리 인덱스 우선 (Memory-first)

작업 시작 시 반드시 메모리 파일을 먼저 읽는다.

- **경로:** `보관함/결과/<YYYY-MM-DD> <지자체> 기금 메모리 인덱스.md`
- 메모리 파일에서 다음을 파악한다:
  - 각 기금별 `Last Updated`(최종 갱신 일자)
  - `Missing Info`(부족한 항목: 조성액·근거 조례·집행 사업 등)
  - `Next Review`(차기 갱신 트리거: 예산안 공고, 조례 개정 등)
- 메모리 파일이 없으면 `보관함/결과/* 기금 *.md`를 스캔해 **초기 인덱스를 생성**한 뒤 진행.
- **금지:** 메모리 확인 없이 광범위 웹 검색·전체 디렉터리 재스캔 수행.

### 2. 갱신 대상 식별 → 타겟 조사

메모리에서 도출된 대상만 조사한다. 일반 검색이 아닌 **구체 키워드**로 타겟팅:

- 좋은 예: `"2026 <지자체> 통합재정안정화기금 운용계획"`, `"<지자체> 도시개발특별회계 2025 결산"`
- 나쁜 예: `"<지자체> 기금"`

**조사 도구·소스:**

- `grep_search`로 워크스페이스 내 기존 자료 우선 확인.
- 대상 URL로 직접 접근해 `보관함/다운로드/<host>/<basename>`에 저장하고 `.meta.json`(source_url, collected_at, SHA-256)을 기록한다.
- 근거 조례 확인은 `collect-ordinance` 산출물 또는 `lawyer`(Legal Data Lookup Priority: Local → MCP → Web) 호출.
- 지방의회 논의는 대상 지자체 의회 회의록(예산결산특별위원회 등) 검색.
- 가능하면 **병렬 호출**로 단축.

### 3. 개별 기금 마크다운 표준 양식

경로: `보관함/결과/<YYYY-MM-DD> <지자체> <기금이름>.md` (파일시스템 충돌 문자는 제거)

```markdown
<!--
[요약 설명]
{기금의 목적과 핵심 운용 방식 1~2문장}

[기금 메타데이터]
- 근거 조례: {조례명 / 제○○호}
- 관리 책임 부서: {부서명}
- 회계 구분: {특별회계 / 기금}
- 최초 설치: {YYYY-MM-DD}
- 태그: {키워드, ...}
-->

# {기금 명칭}

## 1. 기본 정보
- **근거 조례:** {조례명, 링크}
- **운용 목적:** {조항 요약}
- **관리 책임 부서:** {부서}

## 2. 예산 및 운용 규모
| 연도 | 조성액(말잔) | 수입 계획 | 지출 계획 |
| --- | --- | --- | --- |
| 2024 | … | … | … |
| 2025 | … | … | … |

## 3. 주요 집행 사업
- {사업명} — {예산액} — {담당}
- …

## 4. 특정 지역 관련성
- **<생활권/행정동/권역 1>:** {구체 사업·파급 효과}
- **<생활권/행정동/권역 2>:** {…}
- **<생활권/행정동/권역 3>:** {…}
- 해당 없음인 경우 명시(`해당 사항 없음`).

## 5. 특이사항 및 향후 계획
- 시의회 논의: {회의록 링크}
- 조례 개정 동향: {…}

---
출처: {원본 URL} · 수집 {ISO-8601} · sha256:{짧은해시}
```

### 4. 메모리 인덱스 갱신 (필수 마지막 단계)

개별 파일을 갱신했으면 **반드시** 메모리 인덱스의 해당 행을 갱신한다.

```markdown
| 기금명 | 파일 | Last Updated | Missing Info | Next Review |
| --- | --- | --- | --- | --- |
| 통합재정안정화기금 | `보관함/결과/2026-04-30 성남시 통합재정안정화기금.md` | 2026-04-30 | 2026 결산 | 2027-03 결산공고 후 |
```

### 5. 지역 태깅 규칙

지역 연관성(섹션 4)은 **사실 기반**으로만 작성한다.

- 사업비 일부라도 해당 동에 집행되거나, 사업 대상지가 해당 동을 포함할 때만 기재.
- 행정구역 인접·간접효과 추정은 별도 표시(`(추정)`)하고 근거 명시.
- 정치적 해석·평가는 금지(중립 원칙).

## 완료 체크리스트

- [ ] 작업 시작 시 메모리 인덱스를 먼저 읽었는가
- [ ] 갱신 대상이 메모리에서 명확히 식별되었는가(임의 전체 재조사 금지)
- [ ] 원본이 `보관함/다운로드/`에 보존되고 SHA-256이 footer에 기록되었는가
- [ ] 5개 섹션(기본정보·예산·집행사업·지역연관성·특이사항)이 모두 채워졌거나 결측은 메모리의 `Missing Info`에 반영되었는가
- [ ] 메모리 인덱스의 `Last Updated` / `Next Review`가 갱신되었는가
- [ ] PII가 없고 정치적 해석이 본문에 섞이지 않았는가

## 안전·정책

- 외부 호출은 `*.go.kr` 화이트리스트 우선. 신규 도메인은 명시 동의 후 사용.
- robots.txt·rate limit 준수.
- 산출물은 사실 카드(Facts)와 해석(Interpretation)을 분리한다.

## 참고 자원

- `보관함/결과/` — 개별 기금 파일 및 메모리 인덱스(flat)
- 대상 지자체 본청 재정정보공개 포털 (예산안·기금운용계획·결산서)
- 국가법령정보센터 / 자치법규정보시스템 (`elis.go.kr`)
- 대상 지자체 의회 회의록 (예결특위 등)
- 관련 스킬: `collect-ordinance`
- 관련 에이전트: `lawyer` (법령 조회 시 Legal Data Lookup Priority 적용)
